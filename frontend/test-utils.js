/* Re-export @testing-library/react with any shared wrappers.
 * Components needing providers (e.g. ToastContext, i18n) can add
 * a custom render here later. For now, plain re-export suffices. */

export * from '@testing-library/react';
